
<div class="title">Euchre</div>

<a href="<%= user_path(@user) %>">
<button type="button" class="restart_button">Restart Game</button>
</a>

<% case @game_state.current_phase
   when :trump_selection %><%= render partial: 'trump_selection' %>
<% when :discard %><%= render partial: 'discard' %>
<% else %><%= render partial: 'table'  %>
<% end %>
