<div class="player_space">
  <%= render partial: 'games/table/hidden_hand',
      locals: {  player: player } %>
  <div class="won_cards">
    <% player.scored_cards.each do |card| %>
    <%= unicode_card(card) %>
    <% end %>
  </div>
  <div class="player_name"><%= player.name %></div>
</div>
