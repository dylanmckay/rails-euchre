<div class="player_space">
  <div class="hand">
    <% player.hand.each do |card| %>
    <span class="hand_card"><%= unicode_card_back %></span>
    <%end%>
  </div>
  <div class="won_cards">
    <% player.scored_cards.each do |card| %>
    <%= unicode_card(card) %>
    <% end %>
  </div>
  <div class="player_name"><%= player.name %></div>
</div>
