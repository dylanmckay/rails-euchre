<div class="hand">
  <div class="player_space_header">
    <div class="player_points">Points</div>
  </div>

  <% player.hand.each do |card| %>
    <div class="hand_card"> <%= @game.unicode_card(card) %> </div>
  <% end %>
</div>
